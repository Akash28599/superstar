'use strict';

/**
 * scholarship-form controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::scholarship-form.scholarship-form');
