'use strict';

/**
 * scholarship-first service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::scholarship-first.scholarship-first');
